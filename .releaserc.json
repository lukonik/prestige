{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "pnpm -r --filter './packages/*' exec npm version ${nextRelease.version} --no-git-tag-version --allow-same-version",
        "publishCmd": "pnpm -r --filter './packages/*' publish --no-git-checks --access public"
      }
    ],
    "@semantic-release/github"
  ]
}
