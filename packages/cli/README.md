# @prestigia/cli

Create a Prestigia documentation project:

```sh
pnpm dlx @prestigia/cli create my-docs
cd my-docs
pnpm install
pnpm dev
```

The generated project includes TanStack Start, Tailwind CSS, Shadcn components
backed by Base UI, and Content Collections.
