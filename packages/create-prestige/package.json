{
  "name": "@lonik/create-prestige",
  "version": "0.7.0",
  "description": "Scaffold a Prestige application",
  "author": "Luka Onikadze",
  "license": "MIT",
  "bin": {
    "create-prestige": "./dist/index.mjs"
  },
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "release-v": "pnpm run build && bumpp minor --no-commit --no-tag --no-push --no-git-checks && pnpm publish --registry http://localhost:4873 --no-git-checks"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "giget": "^3.1.2",
    "magicast": "^0.3.5",
    "nypm": "^0.6.5",
    "pathe": "catalog:",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "bumpp": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
