---
title: "Customize the interface"
description: "Build your documentation UI with Tailwind CSS and Shadcn Base UI components."
order: 2
---

## Add components

Run `pnpm shadcn add card` to add another component as editable source code.

## Change the theme

Edit the design tokens in `src/styles.css`. Existing components consume those tokens through Tailwind CSS utilities.
