---
title: "Getting started"
description: "Run the project and learn where Prestigia keeps its content."
order: 1
---

## Start the development server

Install dependencies with `pnpm install`, then run `pnpm dev` and open the local URL shown in your terminal.

## Author documentation

Add Markdown files to `content/docs`. Frontmatter is validated by Content Collections before your site builds.
